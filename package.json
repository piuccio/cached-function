{
  "name": "@piuccio/cached-function",
  "version": "1.0.1",
  "description": "Cache the response from any function to a file",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/piuccio/cached-function.git"
  },
  "scripts": {
    "lint": "npx eslint --ignore-path .gitignore .",
    "test": "jest --coverage",
    "test:watch": "jest --watchAll",
    "posttest": "npm run lint"
  },
  "keywords": [
    "cache",
    "async"
  ],
  "engines": {
    "node": ">= 10"
  },
  "author": "Fabio Crisci",
  "license": "MIT",
  "devDependencies": {
    "jest": "26.4.2"
  }
}
